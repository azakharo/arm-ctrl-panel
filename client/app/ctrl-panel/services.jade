#services-page-content.p1
  // Application selector
  #services-app-selector.Grid.Grid--center
    .Grid-cell--autoSize
      label(for="app-select") Выберите приложение:
    .Grid-cell--autoSize.pl1
      select#app-select.form-control(ng-model='appSelected', ng-options='app as app.title for app in apps', ng-change='onSelectedAppChanged()')
  // List of emitters for the selected app
  #services-table-title.text-large.mt2.mb1 Список оказываемых услуг для выбранного приложения:
  // Service list
  #services-tariffs-table-wrapper(ng-hide="gridOptions.data.length === 0")
    .grid(ui-grid="gridOptions", ui-grid-auto-resize, ui-grid-grouping)
  #services-nodata-msg-wrapper(ng-show="gridOptions.data.length === 0")
    .nodata-msg Услуги не найдены
